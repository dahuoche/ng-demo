import { CNgIfDirective } from './c-ng-if.directive';

describe('CNgIfDirective', () => {
  it('should create an instance', () => {
    const directive = new CNgIfDirective();
    expect(directive).toBeTruthy();
  });
});

import { CShowDirective } from './c-show.directive';

describe('CShowDirective', () => {
  it('should create an instance', () => {
    const directive = new CShowDirective();
    expect(directive).toBeTruthy();
  });
});
